#include <stdio.h>

int main(void)
{
	int a = 10, b = 10;

	++a; // increase the variable's value by 1
	--b; // decrease the variable's value by 1

	printf("a : %d\n", a);
	printf("b : %d\n", b);

	return 0;
}